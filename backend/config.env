# ===========================================
# CONFIGURAÇÃO UNIFICADA - FIAP III
# ===========================================

# Configuração do Servidor
PORT=3002
NODE_ENV=development

# MongoDB - API Unificada
MONGODB_URL=mongodb://localhost:27017
MONGODB_DB_NAME=fiap-fase-3
MONGO_PASS=5qJ5u7LmCaauXDrm

# JWT - API Unificada (usando valores reais do .env)
JWT_SECRET=your-secret-key-change-in-production
JWT_EXPIRES_IN=24h
REFRESH_TOKEN_SECRET=your-secret-key-change-in-production
REFRESH_TOKEN_EXPIRES_IN=7d

# Admin padrão (valores reais do .env)
DEFAULT_ADMIN_EMAIL=admin@vehiclesales.com
DEFAULT_ADMIN_PASSWORD=admin123

# Rate Limiting
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100

# CORS (usando valores reais do .env)
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:3003,https://fiap-frontend.onrender.com

# Logs (usando valores reais do .env)
LOG_LEVEL=DEBUG
